# TODO

- [x] Gather context (read Terraform files to understand current infrastructure)

- [x] Update Terraform to create a public subnet (VPC + Internet Gateway + Route Table + Route Table Association)

- [x] Update aws_instance to use the public subnet (so it receives public IP)

- [x] Ensure outputs still return the instance public IP

- [x] Run `terraform init` and `terraform validate` to confirm configuration is valid

- [ ] (Optional) Run `terraform plan` to preview changes

